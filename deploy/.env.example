# Копирай като .env до docker-compose.yml. Не комитвай .env.

# Задължително: openssl rand -hex 32
FMR_JWT_SECRET=
# Срок на JWT в секунди (default 7 дни)
FMR_JWT_TTL_SEC=604800
# Празен = без CORS header. На VPS сложи https://счетоводство.example.com
FMR_CORS=
